# Per-machine mailbox state (durable ownership record + ephemeral lane scratchpads).
# Only the *.example.* templates are tracked.
.mailbox/implementer.json
.mailbox/agent-a.md
.mailbox/agent-b.md
.mailbox/session.log.md
